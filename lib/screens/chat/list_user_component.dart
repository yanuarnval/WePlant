import 'package:flutter/material.dart';

class ListUserComponent extends StatelessWidget {
  const ListUserComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
